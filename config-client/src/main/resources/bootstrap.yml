
---
spring:
  profiles: prod
  application:
    name: config-single-client
  cloud:
    config:
      uri: http://localhost:3301
      label: master
      profile: prod


---
spring:
  profiles: test
  application:
    name: config-clent
  cloud:
    config:
      uri: http://localhost:28055
      label: master
      profile: test